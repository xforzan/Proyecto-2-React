{
  "name": "Proyecto-2-React",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd client && npm install --legacy-peer-deps && cd ../server && npm install --legacy-peer-deps",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm start",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
